<Page x:Class="BreakReminder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:VM="clr-namespace:BreakReminder.ViewModel"
        xmlns:Control="clr-namespace:BreakReminder.Control"
        Title="MainWindow" Height="350" Width="525">
    
    <!--<Page.Resources>
        <DataTemplate DataType="{x:Type VM:ReminderViewModel}" >
            <Control:ReminderConfigControl Content="{Binding}" />
        </DataTemplate>
        <DataTemplate DataType="{x:Type VM:CountDownViewModel}" >
            <Control:CountDownControl Content="{Binding}" />
        </DataTemplate>
    </Page.Resources>
  
    <Border>
        <StackPanel>
            <ContentControl Content="{Binding CurrentView}" />
        </StackPanel>
    </Border>-->
    
    <Control:ReminderConfigControl/>
</Page>
